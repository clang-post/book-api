services:
  book-api:
    build: .
    ports:
      - "3000:3000"
    volumes:
      - book-api-data:/data
    environment:
      - NODE_ENV=production
      - DATABASE_PATH=/data/book_api.db

volumes:
  book-api-data:
